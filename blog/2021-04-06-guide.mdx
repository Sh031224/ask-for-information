---
slug: guide
title: 기여를 위한 가이드
author: Sh031224
author_title: 차승호
author_url: https://github.com/Sh031224
author_image_url: https://avatars.githubusercontent.com/u/51149996?s=48&v=4
tags: [docusaurus, guide, markdown]
---

이 프로젝트에 누구나 기여할 수 있습니다.

## 문서 수정

<!-- <p style={{ display: "flex", alignItems: "center" }}> -->

<span>
	<svg
		fill="currentColor"
		height="1.2em"
		width="1.2em"
		preserveAspectRatio="xMidYMid meet"
		role="img"
		viewBox="0 0 40 40"
		class="iconEdit_node_modules-@docusaurus-theme-classic-lib-next-theme-IconEdit-"
		aria-label="Edit page"
	>
		<g>
			<path d="m34.5 11.7l-3 3.1-6.3-6.3 3.1-3q0.5-0.5 1.2-0.5t1.1 0.5l3.9 3.9q0.5 0.4 0.5 1.1t-0.5 1.2z m-29.5 17.1l18.4-18.5 6.3 6.3-18.4 18.4h-6.3v-6.2z"></path>
		</g>
	</svg>
	Edit this page
</span> 를 클릭 시 해당 파일을 수정하는 <code>Github</code> 로 이동됩니다.

`Github` 에서 `.md` 또는 `.mdx` 확장자에 맞게 문서를 수정하시면 됩니다.

<!-- </p> -->

## 문서 생성

```
ask-for-information
├─ docs
│  └─ category
│     └─ category2
│        └─ doc.md
└─ sidebars.js
```

우리가 주의깊게 살펴봐야 할 부분은 <code>docs</code> 와 <code>sidebars.js</code> 입니다.
기본적으로 모든 문서는 <code>docs</code> 폴더에 있습니다.

또한 문서에 카테고리가 있을 경우 카테고리 이름에 해당하는 폴더를 생성해야합니다.

### 예시

```
ask-for-information
├─ docs
│  └─ example
│     └─ docs.md
└─ sidebars.js
```

### 작성 방법

확장자는 `.md` 또는 `.mdx` 로 작성 가능합니다.

```markdown
---
id: (카테고리-파일이름으로 통일)
title: (제목)
---
```

파일의 제일 상단 부분에 해당 코드를 삽입해야합니다.
또한 파일 이름 네이밍은 `snake case` 로 생성합니다.

### sidebars.js

좌측의 사이드바 관련은 `sidebars.js` 에서 추가하거나 수정해야합니다.

카테고리 추가시

```js
{
	type: "category",
	label: "name",
	items: ["name/name-example"]
}
```

부분을 작성하여 카테고리를 추가 할 수 있습니다.
중첩 카테고리 작성 시 `items` 부분에 위와 같은 코드를 한번 더 적으시면 됩니다.

카테고리가 아닌 문서를 추가하기 위해서는

ex) `docs/interview/frontend.md` 를 추가하려면 `items: ["interview/interview-frontend"]` 를 추가하면 됩니다.

`"folder/docs'id"`

```js
module.export = {
	docs: [
		// 카테고리 없는 아이템
		"example",
		// 카테고리
		{
			type: "category",
			label: "first",
			items: [
				// 아이템
				"first/first-example",
				// 카테고리
				{
					type: "category",
					label: "second",
					items: ["first/second/second-example"]
				}
			]
		},
		// 카테고리
		{
			type: "category",
			label: "third",
			items: ["third/third-example"]
		}
	]
};
```
